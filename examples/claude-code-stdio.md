# Claude Code stdio example

```bash
claude mcp add --transport stdio --scope user proton-mail \
  -- proton-workflow-connector --transport stdio --env-file ~/.config/proton-workflow-connector/env
```

Start Claude Code, then run `/mcp` to check the connection.
